# Contributing to Distributed Storage Simulator

Thank you for considering contributing!

## Code Quality Standards

1. C++20 standard compliance with zero compiler warnings (`-Wall -Wextra -Werror`).
2. Run CTest suite before submitting pull requests (`ctest --test-dir build`).
3. Python control plane code formatted with `ruff` and type-checked with `mypy`.
